"""Package marker for CLI helper scripts included with the Orchestrator repo.

Marking `scripts` as a package makes console entry points like
``scripts.handle_request:main`` importable after installation.
"""
