# Skill Usage Log

Track which skills were used per orchestration cycle and what should be reused later. Entries are populated from Copilot Chat input/output plus runtime invocation hints.

## Entry Template

The skill-usage entry template has been moved to `prompts/skill-usage.prompt.md`.
Use that prompt to append structured skill-usage entries to this file.

---
