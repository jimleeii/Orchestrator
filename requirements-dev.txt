# Development dependencies used for testing, linting and packaging.
# Install these before running build/upload commands:
#
#   python -m pip install --upgrade pip
#   python -m pip install -r requirements-dev.txt

# Packaging and distribution
build>=0.10.0
twine>=4.0.0
wheel>=0.38.4
setuptools>=61.0

# Test runners
pytest>=7.2
pytest-asyncio>=0.21

# Lint/format tools (optional)
ruff
isort
